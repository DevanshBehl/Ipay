# Extension wallet configuration — copy to `.env` (git-ignored).

# Base URL of the InstaPay backend API. Must include the /api suffix.
# Defaults to http://localhost:5001/api if unset.
VITE_API_BASE_URL=http://localhost:5001/api
