# InstaPay Wallet (Chrome MV3 extension)

The browser wallet — see the root [README](../README.md) for the full architecture, security model, and setup.

```bash
npm install && npm run build   # then load web-demo/dist unpacked in chrome://extensions
```
