# InstaPay Checkout demo store config
# Backend gateway base URL (must include /api)
VITE_API_BASE=http://localhost:5001/api
# Merchant key from POST /api/gateway/merchant (can also be pasted in the UI)
VITE_INSTAPAY_KEY=
